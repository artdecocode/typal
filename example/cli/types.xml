<types namespace="_typal">
  <import from="stream" name="TransformOptions"/>
  <type name="Conf" desc="The configuration object.">
    <prop string name="source">
      The source of where to read the data.
    </prop>
    <prop boolean name="closeOnFinish" default="true">
      Closes the stream when done.
    </prop>
  </type>
</types>